[env]
# Include Homebrew in mise's managed PATH so it takes precedence over /usr/bin
_.path = ["/opt/homebrew/bin", "/opt/homebrew/sbin"]

[tools]
node = "latest"
go = "latest"
ruby = "latest"
python = "latest"

[settings]
idiomatic_version_file_enable_tools = ["ruby", "node"]
