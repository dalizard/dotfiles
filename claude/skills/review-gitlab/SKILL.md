---
name: review-gitlab
description: GitLab merge request review. Use when reviewing merge requests in projects hosted on GitLab; always use the glab CLI, never gh.
---

# GitLab Merge Request Review

This project uses GitLab. Always use `glab` CLI, never `gh`.
